# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local env files (real secrets — never commit)
.env

# .env.example is intentionally committed — it's a template, not a secrets file
